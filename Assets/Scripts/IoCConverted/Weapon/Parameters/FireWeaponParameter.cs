using UnityEngine;

public class FireWeaponParameter {

    public Vector2 destination;
    public Vector2 spawnPosition;

    public FireWeaponParameter(Vector2 destination, Vector2 spawnPosition) {
        this.destination = destination;
        this.spawnPosition = spawnPosition;
    }
}
